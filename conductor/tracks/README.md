# Tracks Directory

Each track lives in its own folder:

```
conductor/tracks/<track_id>/
  spec.md
  plan.md
  metadata.json
```

Track IDs should be short and dated, e.g. `integration_tests_20260121`.

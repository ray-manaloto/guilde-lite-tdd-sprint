---
name: claude-diary
description: Use claude-diary to capture session traces and reflections for analysis and documentation.
metadata:
  short-description: Claude diary workflow
---

# Claude Diary

Use this skill when you need to capture interaction traces or post-session
summaries using the `claude-diary` tool.

## Workflow

1. Use `scripts/install-claude-diary.sh` to install commands/hooks and settings.
2. Run `/diary` and `/reflect` in Claude Code as needed.
3. Store outputs in the agreed repo location and link them in docs if needed.

## References

- See `references/claude-diary.md` for links and context.
